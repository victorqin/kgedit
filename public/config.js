// ---------------------------------------------------------------------------
// 运行期配置。构建一次，部署到任意环境只需替换本文件，无需重新构建。
// apiBaseUrl 留空则回退到构建期的 VITE_API_BASE_URL。
//
// CI/CD 示例：
//   echo "window.__APP_CONFIG__={apiBaseUrl:'$API_BASE'};" > dist/config.js
// ---------------------------------------------------------------------------
window.__APP_CONFIG__ = {
  apiBaseUrl: '',
}
